<div id="fun">
	<div class="content">
		<h1>{{global.index.sections[3].title}}</h1>
		<p>{{global.index.sections[3].intro}}</p>
	
		<ul class="fun">
		{% for item in global.index.sections[3].gallery %}
			<li>
				<a href="{{item.url}}"><img src="{{item.imageUrl}}" alt="{{item.name}}"></a>
			</li>
		{% endfor %}
		</ul>
	</div>
</div>